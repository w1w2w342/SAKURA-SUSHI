# 🍣 SAKURA SUSHI — Premium Landing Page

**Описание проекта**

Это современный одностраничный сайт (Landing Page) для премиального ресторана суши, написанный на чистом **HTML5**, **CSS3** и **Vanilla JavaScript** (без использования фреймворков).

Сайт выполнен в темной теме с использованием эффектов "стекломорфизма" (Glassmorphism), золотых акцентов и плавных анимаций. Реализована полная имитация работы интернет-магазина: генерация меню из JavaScript-массива, фильтрация товаров, полноценная корзина с подсчетом стоимости и модальные окна. Также присутствует блок с имитацией скачивания мобильного приложения.

---

## ⚙️ Функции кода (script.js)

В проекте реализована модульная логика на чистом JS. Основные функции:

* **`sushiData`** — Массив объектов, имитирующий базу данных. Содержит информацию о товарах (id, название, цена, вес, ингредиенты, фото).
* **`renderMenu(filter)`** — Основная функция рендеринга. Очищает сетку товаров и заново создает HTML-карточки на основе выбранного фильтра (`premium`, `classic`, `baked` и т.д.).
* **`addToCart(id)`** — Добавляет товар в массив корзины. Если товар уже есть, увеличивает его количество (`qty`). Вызывает обновление интерфейса.
* **`updateCartUI()`** — Полностью перерисовывает боковую панель корзины. Считает общую сумму заказа, генерирует HTML для списка выбранных товаров и обновляет счетчик на иконке корзины.
* **`changeQty(id, change)`** — Управляет количеством товара (+1 или -1). Если количество становится равным нулю, товар удаляется из корзины.
* **`observeElements()`** — Инициализирует `IntersectionObserver`. Отслеживает появление элементов на экране при скролле и добавляет им класс `.visible` для плавного всплытия (анимации).
* **`filterBtns`** — Логика кнопок фильтрации. Переключает активный класс и вызывает `renderMenu` с нужной категорией.
* **`openCart` / `closeCart`** — Управляет CSS-классами для открытия и закрытия боковой панели корзины (выезжает справа).
* **`checkoutBtn`** — Обработчик кнопки "Оформить заказ". Проверяет, не пуста ли корзина, очищает её и открывает модальное окно благодарности.

---

## 🎨 Особенности дизайна (style.css)

* **Glassmorphism Header:** Шапка сайта имеет полупрозрачный фон с размытием (`backdrop-filter: blur`), который меняет прозрачность при прокрутке страницы.
* **CSS Variables:** Все цвета, шрифты и размеры вынесены в переменные (`:root`) для легкой смены темы.
* **Responsive Grid:** Меню адаптируется под любые экраны благодаря `display: grid` и `minmax`.
* **Dark Mode UI:** Глубокий черный фон с золотыми (`#cca43b`) и красными (`#e63946`) акцентами.

---

## 🚀 Как использовать

1.  **Структура файлов:**
    Создайте папку и поместите в неё три файла:
    * `index.html` (код структуры)
    * `style.css` (код стилей)
    * `script.js` (код логики)

2.  **Файл для скачивания (Опционально):**
    Чтобы кнопка "Скачать Beta" работала корректно, положите в папку с сайтом любой файл с названием `sushi-beta.exe` (или измените имя файла в HTML на строке 286).

3.  **Запуск:**
    Просто откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Edge).

4.  **Использование:**
    * Навигация работает через плавный скролл.
    * Добавляйте товары в корзину, меняйте их количество.
    * Используйте фильтры "Премиум", "Классика" и др. для сортировки меню.
    * Нажмите "Оформить заказ" для проверки работы модального окна.

---

## 🛠️ Стек технологий

* **HTML5** (Семантическая верстка, BEM-нейминг классов)
* **CSS3** (Flexbox, Grid, Media Queries, Animation, Variables)
* **JavaScript ES6+** (Arrow functions, DOM Manipulation, Array Methods)
* **RemixIcon** (Иконочный шрифт)
* **Google Fonts** (Inter, Playfair Display)
* я не отвечаю за ващ пк
* 
